# 主环境(conda py3.12):LangChain/LangGraph 编排 + angr/unicorn/z3 求解。
# 装法: conda create -y -n antirev python=3.12 && conda run -n antirev pip install -r requirements-main.txt
# 完整锁定(可复现/离线 wheelhouse 用): requirements-lock.txt
# 注: keystone-engine 在本机 dylib 加载失败,MVP 四工具不依赖它;需要时另行修复。
langchain==1.3.14
langgraph==1.2.9
langchain-openai==1.3.5
openai==2.46.0
angr==9.3.0
claripy==9.3.0
z3-solver==4.13.0.0
unicorn==2.1.2
capstone==5.0.6
keystone-engine==0.9.2
pwntools==4.15.0
pydantic==2.13.4
requests==2.34.2
pytest==9.1.1
